!.*
.git/
.pnpm-store/
lib/
node_modules/
LICENSE/
package.json
pnpm-lock.yaml

# Japanese has no word boundaries for prettier to wrap at, so `proseWrap`
# joins the whole paragraph into one line that then trips the line-length
# rule. It is wrapped by hand instead.
VISION.jp.md
